"""

idea: why not just one big numpy evaluate? numexpr

This would permit presenting the tree as a string (with nested structure)..

Decision tree has six threshold quantities:
    - 3 bands: TM1 (blue), TM3 (red), TM7 (swir2)
    - 3 band-ratio indices: NDI52, NDI43, NDI72

"""
